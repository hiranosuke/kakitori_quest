# Phase 5-A PWA Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** kakitori Quest を PWA 化し、ホーム画面追加・CDN ランタイムキャッシュ・オフライン時バッジ表示を実現する

**Architecture:** `vite-plugin-pwa` で Web App Manifest と Service Worker（Workbox）を生成。CDN（cdn.jsdelivr.net）への Stale-While-Revalidate キャッシュで一度プレイした単語はオフラインでも動作させる。アプリ本体の precache はしない。`useOnlineStatus` フックで状態を検知し、`OfflineBadge` コンポーネントをすべての画面上に fixed 表示する。

**Tech Stack:** React 18, TypeScript, Vite, vite-plugin-pwa (Workbox)

---

## File Structure

**新規作成:**
- `src/hooks/useOnlineStatus.ts` — `navigator.onLine` + イベント監視で boolean を返すフック
- `src/components/ui/OfflineBadge.tsx` — バッジ UI + タップ時ポップアップ
- `src/__tests__/hooks/useOnlineStatus.test.ts` — フックのテスト
- `src/__tests__/components/OfflineBadge.test.tsx` — コンポーネントのテスト
- `public/icons/icon-192.png` — PWA アイコン（手動配置）
- `public/icons/icon-512.png` — PWA アイコン（手動配置）

**既存ファイルの修正:**
- `package.json` — `vite-plugin-pwa` を devDependency に追加
- `vite.config.ts` — VitePWA プラグイン設定を追加
- `index.html` — `<meta name="theme-color">` を追加
- `src/config/messages.ts` — `MSG.offline` を追加
- `src/App.tsx` — `OfflineBadge` を組み込む

---

## Task 1: vite-plugin-pwa のインストールと設定

**Files:**
- Modify: `package.json`
- Modify: `vite.config.ts`
- Modify: `index.html`

- [ ] **Step 1: vite-plugin-pwa をインストール**

  ```bash
  npm install -D vite-plugin-pwa
  ```

  Expected: `package.json` の `devDependencies` に `"vite-plugin-pwa"` が追加される

- [ ] **Step 2: vite.config.ts を更新**

  ```ts
  import { defineConfig } from 'vitest/config'
  import react from '@vitejs/plugin-react'
  import { VitePWA } from 'vite-plugin-pwa'

  export default defineConfig({
    plugins: [
      react(),
      VitePWA({
        registerType: 'autoUpdate',
        manifest: {
          name: 'かきとりクエスト',
          short_name: 'かきとりQ',
          display: 'standalone',
          background_color: '#000000',
          theme_color: '#000000',
          start_url: '/kakitori_quest/',
          scope: '/kakitori_quest/',
          lang: 'ja',
          icons: [
            {
              src: 'icons/icon-192.png',
              sizes: '192x192',
              type: 'image/png',
            },
            {
              src: 'icons/icon-512.png',
              sizes: '512x512',
              type: 'image/png',
            },
          ],
        },
        workbox: {
          runtimeCaching: [
            {
              urlPattern: /^https:\/\/cdn\.jsdelivr\.net\/.*/i,
              handler: 'StaleWhileRevalidate',
              options: {
                cacheName: 'cdn-kanji-data',
                expiration: {
                  maxEntries: 200,
                  maxAgeSeconds: 60 * 60 * 24 * 30,
                },
              },
            },
          ],
        },
      }),
    ],
    base: '/kakitori_quest/',
    test: {
      environment: 'jsdom',
      globals: true,
      setupFiles: ['./src/__tests__/setup.ts'],
    },
  })
  ```

- [ ] **Step 3: index.html に theme-color メタタグを追加**

  `<link rel="icon" ...>` の直後に追加：

  ```html
  <meta name="theme-color" content="#000000" />
  ```

- [ ] **Step 4: ビルドが通ることを確認**

  ```bash
  npm run build && ls dist/manifest.webmanifest dist/sw.js
  ```

  Expected: `dist/manifest.webmanifest` と `dist/sw.js` が生成されている（アイコンは後続タスクで配置）

- [ ] **Step 5: Commit**

  ```bash
  git add package.json package-lock.json vite.config.ts index.html
  git commit -m "feat: install vite-plugin-pwa and configure Web App Manifest with CDN runtime caching"
  ```

---

## Task 2: アイコンの配置と MSG.offline の追加

**Files:**
- Create: `public/icons/icon-192.png`（手動）
- Create: `public/icons/icon-512.png`（手動）
- Modify: `src/config/messages.ts`

- [ ] **Step 1: icons ディレクトリを作成し、アイコンを配置**

  カスタムアイコン PNG を用意して以下のパスに配置する：

  ```
  public/icons/icon-192.png  （192×192px）
  public/icons/icon-512.png  （512×512px）
  ```

  開発中の仮置きが必要な場合は ImageMagick で黒い正方形を生成できる：

  ```bash
  mkdir -p public/icons
  convert -size 192x192 xc:#1a1a1a public/icons/icon-192.png
  convert -size 512x512 xc:#1a1a1a public/icons/icon-512.png
  ```

- [ ] **Step 2: messages.ts に MSG.offline を追加**

  `src/config/messages.ts` の `MSG` オブジェクト末尾（`world:` ブロックの後）に追加：

  ```ts
    offline: {
      badge: 'オフライン',
      title: '📵 オフラインです',
      body1: 'いちどあそんだことばは',
      body2: 'そのままつかえます。',
      body3: 'はじめてのことばには',
      body4: 'インターネットが',
      body5: 'ひつようです。',
      close: 'とじる',
    },
  ```

- [ ] **Step 3: TypeScript エラーなし確認**

  ```bash
  npx tsc --noEmit
  ```

  Expected: エラーなし

- [ ] **Step 4: Commit**

  ```bash
  git add public/icons/ src/config/messages.ts
  git commit -m "feat: add PWA icons and offline message strings"
  ```

---

## Task 3: useOnlineStatus フックの実装（TDD）

**Files:**
- Create: `src/__tests__/hooks/useOnlineStatus.test.ts`
- Create: `src/hooks/useOnlineStatus.ts`

- [ ] **Step 1: テストファイルを作成**

  `src/__tests__/hooks/useOnlineStatus.test.ts`:

  ```ts
  import { renderHook, act } from '@testing-library/react'
  import { vi } from 'vitest'
  import { useOnlineStatus } from '../../hooks/useOnlineStatus'

  describe('useOnlineStatus', () => {
    it('初期値は navigator.onLine を返す', () => {
      const { result } = renderHook(() => useOnlineStatus())
      expect(result.current).toBe(navigator.onLine)
    })

    it('offline イベントで false になる', () => {
      const { result } = renderHook(() => useOnlineStatus())
      act(() => {
        window.dispatchEvent(new Event('offline'))
      })
      expect(result.current).toBe(false)
    })

    it('offline → online イベントで true に戻る', () => {
      const { result } = renderHook(() => useOnlineStatus())
      act(() => {
        window.dispatchEvent(new Event('offline'))
      })
      act(() => {
        window.dispatchEvent(new Event('online'))
      })
      expect(result.current).toBe(true)
    })

    it('アンマウント時にイベントリスナーを解除する', () => {
      const removeSpy = vi.spyOn(window, 'removeEventListener')
      const { unmount } = renderHook(() => useOnlineStatus())
      unmount()
      expect(removeSpy).toHaveBeenCalledWith('online', expect.any(Function))
      expect(removeSpy).toHaveBeenCalledWith('offline', expect.any(Function))
      removeSpy.mockRestore()
    })
  })
  ```

- [ ] **Step 2: テストが失敗することを確認**

  ```bash
  npx vitest run src/__tests__/hooks/useOnlineStatus.test.ts
  ```

  Expected: FAIL（`useOnlineStatus` が存在しないため）

- [ ] **Step 3: useOnlineStatus.ts を実装**

  `src/hooks/useOnlineStatus.ts`:

  ```ts
  import { useEffect, useState } from 'react'

  export function useOnlineStatus(): boolean {
    const [isOnline, setIsOnline] = useState(navigator.onLine)

    useEffect(() => {
      const handleOnline = () => setIsOnline(true)
      const handleOffline = () => setIsOnline(false)

      window.addEventListener('online', handleOnline)
      window.addEventListener('offline', handleOffline)

      return () => {
        window.removeEventListener('online', handleOnline)
        window.removeEventListener('offline', handleOffline)
      }
    }, [])

    return isOnline
  }
  ```

- [ ] **Step 4: テストが通ることを確認**

  ```bash
  npx vitest run src/__tests__/hooks/useOnlineStatus.test.ts
  ```

  Expected: 4 tests PASS

- [ ] **Step 5: 全テスト確認**

  ```bash
  npx vitest run
  ```

  Expected: 全テスト PASS

- [ ] **Step 6: Commit**

  ```bash
  git add src/hooks/useOnlineStatus.ts src/__tests__/hooks/useOnlineStatus.test.ts
  git commit -m "feat: add useOnlineStatus hook with online/offline event tracking"
  ```

---

## Task 4: OfflineBadge コンポーネントの実装（TDD）

**Files:**
- Create: `src/__tests__/components/OfflineBadge.test.tsx`
- Create: `src/components/ui/OfflineBadge.tsx`

- [ ] **Step 1: テストファイルを作成**

  `src/__tests__/components/OfflineBadge.test.tsx`:

  ```tsx
  import { render, screen, fireEvent } from '@testing-library/react'
  import { vi } from 'vitest'
  import { OfflineBadge } from '../../components/ui/OfflineBadge'
  import * as onlineStatusModule from '../../hooks/useOnlineStatus'

  vi.mock('../../hooks/useOnlineStatus')
  const mockUseOnlineStatus = vi.mocked(onlineStatusModule.useOnlineStatus)

  describe('OfflineBadge', () => {
    it('オンライン時は何も表示しない', () => {
      mockUseOnlineStatus.mockReturnValue(true)
      const { container } = render(<OfflineBadge />)
      expect(container.firstChild).toBeNull()
    })

    it('オフライン時はバッジボタンを表示する', () => {
      mockUseOnlineStatus.mockReturnValue(false)
      render(<OfflineBadge />)
      expect(screen.getByRole('button', { name: /オフライン/ })).toBeInTheDocument()
    })

    it('バッジをタップするとポップアップが開く', () => {
      mockUseOnlineStatus.mockReturnValue(false)
      render(<OfflineBadge />)
      fireEvent.click(screen.getByRole('button', { name: /オフライン/ }))
      expect(screen.getByText('いちどあそんだことばは')).toBeInTheDocument()
    })

    it('とじるボタンでポップアップが閉じる', () => {
      mockUseOnlineStatus.mockReturnValue(false)
      render(<OfflineBadge />)
      fireEvent.click(screen.getByRole('button', { name: /オフライン/ }))
      fireEvent.click(screen.getByRole('button', { name: /とじる/ }))
      expect(screen.queryByText('いちどあそんだことばは')).not.toBeInTheDocument()
    })

    it('オーバーレイをタップするとポップアップが閉じる', () => {
      mockUseOnlineStatus.mockReturnValue(false)
      render(<OfflineBadge />)
      fireEvent.click(screen.getByRole('button', { name: /オフライン/ }))
      fireEvent.click(screen.getByTestId('offline-overlay'))
      expect(screen.queryByText('いちどあそんだことばは')).not.toBeInTheDocument()
    })
  })
  ```

- [ ] **Step 2: テストが失敗することを確認**

  ```bash
  npx vitest run src/__tests__/components/OfflineBadge.test.tsx
  ```

  Expected: FAIL（`OfflineBadge` が存在しないため）

- [ ] **Step 3: OfflineBadge.tsx を実装**

  `src/components/ui/OfflineBadge.tsx`:

  ```tsx
  import { useState } from 'react'
  import { useOnlineStatus } from '../../hooks/useOnlineStatus'
  import { MSG } from '../../config/messages'
  import { DQWindow } from './DQWindow'

  export function OfflineBadge() {
    const isOnline = useOnlineStatus()
    const [open, setOpen] = useState(false)

    if (isOnline) return null

    return (
      <>
        <button
          aria-label={MSG.offline.badge}
          onClick={() => setOpen(true)}
          style={{
            position: 'fixed',
            top: '8px',
            left: '8px',
            zIndex: 9999,
            background: '#111',
            border: '1px solid var(--color-accent)',
            borderRadius: '4px',
            color: 'var(--color-accent)',
            fontFamily: 'var(--font-pixel)',
            fontSize: '0.7em',
            padding: '4px 8px',
            cursor: 'pointer',
          }}
        >
          📵 {MSG.offline.badge}
        </button>

        {open && (
          <>
            <div
              data-testid="offline-overlay"
              onClick={() => setOpen(false)}
              style={{
                position: 'fixed',
                inset: 0,
                zIndex: 10000,
                background: 'rgba(0,0,0,0.6)',
              }}
            />
            <div
              style={{
                position: 'fixed',
                top: '50%',
                left: '50%',
                transform: 'translate(-50%, -50%)',
                zIndex: 10001,
                width: '280px',
              }}
            >
              <DQWindow>
                <p style={{ color: 'var(--color-accent)', marginBottom: '12px' }}>
                  {MSG.offline.title}
                </p>
                <p style={{ fontSize: '0.9em', lineHeight: 1.8, marginBottom: '16px' }}>
                  {MSG.offline.body1}<br />
                  {MSG.offline.body2}<br />
                  <br />
                  {MSG.offline.body3}<br />
                  {MSG.offline.body4}<br />
                  {MSG.offline.body5}
                </p>
                <button
                  onClick={() => setOpen(false)}
                  style={{
                    display: 'block',
                    width: '100%',
                    background: 'none',
                    border: 'none',
                    color: 'var(--color-text)',
                    fontFamily: 'var(--font-pixel)',
                    fontSize: '1em',
                    padding: '8px',
                    cursor: 'pointer',
                  }}
                >
                  ▶　{MSG.offline.close}
                </button>
              </DQWindow>
            </div>
          </>
        )}
      </>
    )
  }
  ```

- [ ] **Step 4: テストが通ることを確認**

  ```bash
  npx vitest run src/__tests__/components/OfflineBadge.test.tsx
  ```

  Expected: 5 tests PASS

- [ ] **Step 5: 全テスト確認**

  ```bash
  npx vitest run
  ```

  Expected: 全テスト PASS

- [ ] **Step 6: Commit**

  ```bash
  git add src/components/ui/OfflineBadge.tsx src/__tests__/components/OfflineBadge.test.tsx
  git commit -m "feat: add OfflineBadge with fixed indicator and offline explanation popup"
  ```

---

## Task 5: App.tsx への組み込みと最終確認

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: App.tsx を更新して OfflineBadge を組み込む**

  `src/App.tsx` を以下に書き換える：

  ```tsx
  import { useGameStore } from './store/gameStore'
  import { TitleScreen } from './screens/TitleScreen'
  import { WorldSelectScreen } from './screens/WorldSelectScreen'
  import { StageSelectScreen } from './screens/StageSelectScreen'
  import { GameScreen } from './components/game/GameScreen'
  import { StageCompleteScreen } from './screens/StageCompleteScreen'
  import { GameOverScreen } from './screens/GameOverScreen'
  import { SettingsScreen } from './screens/SettingsScreen'
  import { ShopScreen } from './screens/ShopScreen'
  import { WardrobeScreen } from './screens/WardrobeScreen'
  import { WorldClearScreen } from './screens/WorldClearScreen'
  import { OfflineBadge } from './components/ui/OfflineBadge'

  export default function App() {
    const screen = useGameStore((s) => s.screen)

    function renderScreen() {
      switch (screen) {
        case 'title':         return <TitleScreen />
        case 'world-select':  return <WorldSelectScreen />
        case 'stageSelect':   return <StageSelectScreen />
        case 'game':          return <GameScreen />
        case 'stageComplete': return <StageCompleteScreen />
        case 'gameOver':      return <GameOverScreen />
        case 'settings':      return <SettingsScreen />
        case 'shop':          return <ShopScreen />
        case 'wardrobe':      return <WardrobeScreen />
        case 'world-clear':   return <WorldClearScreen />
        default:              return <TitleScreen />
      }
    }

    return (
      <>
        {renderScreen()}
        <OfflineBadge />
      </>
    )
  }
  ```

- [ ] **Step 2: TypeScript エラーなし確認**

  ```bash
  npx tsc --noEmit
  ```

  Expected: エラーなし

- [ ] **Step 3: 全テスト確認**

  ```bash
  npx vitest run
  ```

  Expected: 全テスト PASS（既存 72 + 新規 9 = 81 tests）

- [ ] **Step 4: ビルド確認**

  ```bash
  npm run build && ls dist/manifest.webmanifest dist/sw.js
  ```

  Expected: ビルド成功、`manifest.webmanifest` と `sw.js` が存在する

- [ ] **Step 5: Commit**

  ```bash
  git add src/App.tsx
  git commit -m "feat: integrate OfflineBadge into App and complete Phase 5-A PWA"
  ```

---

## Self-Review

### Spec Coverage チェック

| 仕様要件 | 対応タスク |
|---|---|
| vite-plugin-pwa インストール | Task 1 |
| Web App Manifest（name/short_name/display/colors/start_url/icons） | Task 1 |
| CDN Stale-While-Revalidate ランタイムキャッシュ | Task 1 |
| theme-color メタタグ | Task 1 |
| PWA アイコン 192×192 / 512×512 | Task 2 |
| MSG.offline 文言 | Task 2 |
| useOnlineStatus フック | Task 3 |
| OfflineBadge（オフライン時のみ、fixed 左上） | Task 4 |
| バッジタップでポップアップ表示 | Task 4 |
| とじるボタン・オーバーレイで閉じる | Task 4 |
| App.tsx への組み込み | Task 5 |

スコープ外: apple-touch-icon・プッシュ通知・完全オフライン対応 → 対応なし（正しい）

### 型一貫性チェック

- `useOnlineStatus(): boolean` → Task 3 定義、Task 4 で import ✓
- `MSG.offline.badge / title / body1-5 / close` → Task 2 定義、Task 4 で参照 ✓
- `data-testid="offline-overlay"` → Task 4 テストと実装で一致 ✓
